% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditionaleffect.R
\name{conditionaleffect}
\alias{conditionaleffect}
\title{Regression-based estimators for conditional average treatment effect}
\usage{
conditionaleffect(data, y, o, d, x, estimator = "Lrnr_glm", boot_reps = 500)
}
\arguments{
\item{data}{A dataframe}

\item{y}{The outcome variable (string), numeric or factor}

\item{o}{The origin variable (string), must be a factor}

\item{d}{The destination/treatment variable (string), must be a factor with >=2 levels}

\item{x}{A vector of strings for additional control variables (confounders)}

\item{estimator}{A learner name or vector of learner names from \code{sl3}. If multiple,
a Super Learner is built. Default is "Lrnr_glm".}

\item{boot_reps}{The number of bootstrap repetitions for estimating standard errors. Default is 500.}
}
\value{
A list containing:
\item{predicted_outcomes}{A dataframe of predicted outcomes, each column corresponding to a treatment level.}
\item{cate_summary}{A dataframe of conditional average treatment effects (CATEs) with bootstrapped standard errors.}
}
\description{
This function fits a Super Learner (through \code{sl3}) to estimate the outcome model,
then predicts outcomes for each treatment level to derive conditional average
treatment effects (CATEs). It can optionally bootstrap these estimates.
}
\examples{
\dontrun{
  result <- conditionaleffect(
    data = my_data,
    y = "outcome",
    o = "origin",
    d = "treatment",
    x = c("control1", "control2"),
    estimator = c("Lrnr_glm", "Lrnr_randomForest"),
    boot_reps = 1000
  )
}
}
